const initialState = {
    songs: [],
    loading: true
}

const songsReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default songsReducer;