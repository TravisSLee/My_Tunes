const initialState = {
    songs: [],
    loading: true
}

const songsReducer = (state, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default songsReducer;